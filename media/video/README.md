# Local round videos

Video files remain local to the Display Mac and are intentionally ignored by
Git. Put the supplied `.mp4` files in this folder using their existing names.

The public Host sends only playback commands through Firebase. The local
Display resolves and plays the files; Player devices never download them.
